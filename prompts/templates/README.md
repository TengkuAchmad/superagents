# Prompt Templates

Canonical location for all shared prompt templates.

| File | Purpose |
|------|---------|
| `system_prompt.txt` | Base system prompt injected into all agents |
| `task_prompt.txt` | Task instruction wrapper template |
| `mcp_retry_logic.txt` | MCP retry/fallback logic instructions |
